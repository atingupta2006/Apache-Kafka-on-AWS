# Day 1 assignment — topic card

Use **only your** resources. Example if you are `user3`: topic `orders-user3`, group `cg-user3-support`.

Fill this table from the lab commands (topic `--describe` and consumer group `--describe`). Short answers are fine.

| Item | Your answer |
|------|-------------|
| Your login id (`userN`) | |
| Topic name | |
| Consumer group name | |
| How many partitions? | |
| Replication factor (RF) | |
| Leader broker id for each partition | |
| Replicas / ISR (healthy? yes/no) | |
| Did your group appear after consume? | |
| Message flow in one simple sentence | |

**Message flow hint:** “Producer writes to the topic; Kafka stores it on brokers; my consumer group reads it and saves an offset.”
